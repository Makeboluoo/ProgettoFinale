package it.polimi.zagardo.progettofinale.model.enums;

public enum Role {
    // ci sono solo 4 classi di utenti all'interno di un gruppo, di cui 3 sono attive e 1 è passiva (waiting)
    Administrator, Senior, Junior , Waiting
}
